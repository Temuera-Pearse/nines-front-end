# Background Assets

Store full-scene visuals here.

Suggested contents:

- `race-track/track-base.*`
- `race-track/crowd-overlay.*`
- `race-track/sky-overlay.*`

The existing `raceTrack.jpg` can be migrated here later once runtime imports are updated.
